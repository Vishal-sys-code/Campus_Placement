#include <bits/stdc++.h>
using namespace std;
#define MAX 50

int main(){
    string s;
    cin>>s;
    int len = s.length();
    cout << "The number of letters in the name is " << len << endl;
    return 0;
}